\textbf{Alice mide sus 2 qubits, obteniendo 2 bits clásicos}\vspace{.3cm}
\begin{align*}
    a,b \in \{0,1\}
\end{align*}
Aquí:
\begin{align*}
    a = \text{ resultado de medir el primer qubit, } && b = \text{ resultado de medir el segundo
    qubit.}
\end{align*}

Dados los cuatros posibles resultados, determine qué operación debe aplicar Bob para reconstruir el
estado original $\ket{\chi}$. Escriba la operación correctiva explícitamente en función de los
parámetros $m,n$ del estado de Bell inicial, y de los bits de medición $a,b$.

\textcolor{bibi}{}
\begin{quote}
\end{quote}
