\textbf{Aplique $U(t)$ al estado inicial $\ket{0}$ y obtenga el estado resultante.} \vspace{.3cm}

\begin{align*}
    U(t)\ket{0} = (cos(t) \ I- i \ sin(t) \ X) \begin{pmatrix}1 \\ 0\end{pmatrix} =
    \begin{pmatrix}cos(t) \\ -i \ sin(t)\end{pmatrix}
\end{align*}
